import React from "react";
import { Text } from "native-base";
import styleText from "../style/styleText"

const Header = () => {
    return (
        <Text style = {styleText.Header}>Edit Task</Text>
    )
}

export default Header;