import { StyleSheet } from "react-native";

export default StyleSheet.create ({
    header : {
        textAlign : "center",
        padding : 10,
        marginBottom : 15,
        fontSize : 25,
        fontWeight : "bold"
    },

    stack : {
        justifyContent : "center",
        marginTop : "20%"
    },

    selector : {
        borderRadius : 10,
        marginBottom : 10
    },

    boxBtn : {
        flexDirection : "row",
        marginTop : 70
    },

    text : {
        marginBottom : 10
    }
})